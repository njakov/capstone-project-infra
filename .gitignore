# --- Terraform ---
# Local .terraform directories (plugins, modules)
**/.terraform/

# Terraform State files (We use GCS remote backend, so never commit local state)
*.tfstate
*.tfstate.*
*.tfstate.backup
errored.tfstate

# Crash logs
crash.log
crash.log.*

*.tfvars
*.tfvars.json

!environments/dev/terraform.tfvars
!environments/prod/terraform.tfvars
!environments/bootstrap/dev.tfvars
!environments/bootstrap/prod.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- GCP & Secrets (CRITICAL) ---
# Service Account Keys (JSON files)
*.json
!package.json  # Allow package.json if you ever use Node.js
!policies.json # Allow policy definitions if needed

# SSH Keys
*.pem
*.key
*.pub
id_rsa

# --- Ansible ---
*.retry
inventory/  # Only ignore if using dynamic inventory scripts generating files here

# --- Python (For your scripts) ---
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# --- IDE & System Files ---
# Mac
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# VS Code
.vscode/

# IntelliJ / JetBrains
.idea/
*.iml

ssh-to-runner.txt
copilot-instructions.md